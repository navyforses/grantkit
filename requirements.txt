# Python dependencies for GrantKit utility scripts.
#
# Install with:  pip install -r requirements.txt

# auth.py — secure password hashing (replaces insecure MD5).
# bcrypt is a purpose-built, intentionally slow algorithm that resists
# brute-force and rainbow-table attacks.
bcrypt>=4.0.0
